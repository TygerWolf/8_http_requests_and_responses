<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
    <li><%= contact.id %> <%= contact.first_name %> <%= contact.last_name %> / <%= contact.email%> / <%= contact.note %> <a href="/contacts/<%= contact.id %>/edit">Edit</a></li>
  <% end %>
</ul>


